#ifndef VECTOR_OPERATIONS_H
#define VECTOR_OPERATIONS_H

double sum_vector_elem(double *, size_t);

double *vector_sub_scalar(double *, size_t, double);

double *multi_by_element(double *, double *, size_t);

#endif